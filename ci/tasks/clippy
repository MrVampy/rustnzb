#!/bin/sh
set -eu
. "$(dirname "$0")/lib.sh"
trap cleanup_frontend EXIT
task_start clippy cargo
task_target_dir clippy
prepare_placeholder_frontend
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
show_sccache_stats
